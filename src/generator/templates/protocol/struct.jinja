{% set s = def.Struct %}
#[derive(Debug, Default, Clone, Copy)]
#[repr(C)]
pub struct {{ s.name }} {
    {% for member in s.members -%}
    pub {{ member.name }}: {{ member.type_name }},
    {% endfor %}
}
